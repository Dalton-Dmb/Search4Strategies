# KPM-18 Merge Note

After CI passes, merge this PR to make the handoff available on `main`. The successor should then branch from the new `main` head and record that exact SHA in the first release/evidence manifest.
