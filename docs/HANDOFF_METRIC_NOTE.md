# AlphaIQ™ Metric Semantics Handoff

KPM-08 historically exposed per-trade `sharpe_like`/`sortino_like` values. KPM-12 adds explicit time-normalised/annualised statistical policy. Do not relabel the former as conventional annualised Sharpe.

Always state return sampling/annualisation assumptions, sample size and uncertainty. Prefer conservative confidence-bound evidence for research ranking and preserve drawdown/tail/excursion/execution-cost diagnostics. A composite Instrument Productivity score is a configured research prioritisation metric; its weights and inputs must remain explicit and versioned.
